[build]
  publish = "."
  functions = "netlify/functions"

# Keep the headers minimal; no CSP to avoid blocking scripts/styles.
[[headers]]
  for = "/*"
  [headers.values]
  Access-Control-Allow-Origin = "*"